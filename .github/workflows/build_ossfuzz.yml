# Build OSSFuzz fuzz targets from source.
name: build_ossfuzz
on:
  pull_request:
  push:
permissions: read-all
jobs:
  build_ossfuzz:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ["ubuntu-24.04"]
    steps:
    - name: Install build dependencies
      run: |
        sudo apt -y install git
    - uses: actions/checkout@v6
      with:
        repository: google/oss-fuzz
        path: oss-fuzz
    - name: Build OSSFuzz fuzz targets
      env:
        BRANCH: ${{ github.head_ref || github.ref_name }}
        ORGANIZATION: ${{ github.event.pull_request.head.repo.owner.login || github.repository_owner }}
      working-directory: oss-fuzz
      run: |
        mkdir -p projects/libsigscan
        cp projects/libyal/build.sh projects/libsigscan/
        cp projects/libyal/project.yaml projects/libsigscan/
        head -n 20 projects/libyal/Dockerfile > projects/libsigscan/Dockerfile
        echo "RUN git clone --depth 1 https://github.com/${ORGANIZATION:-libyal}/libsigscan.git -b ${BRANCH:-main} libsigscan" >> projects/libsigscan/Dockerfile
        tail -n 3 projects/libyal/Dockerfile >> projects/libsigscan/Dockerfile
        python3 infra/helper.py build_image --pull libsigscan
        python3 infra/helper.py build_fuzzers --sanitizer address libsigscan
        python3 infra/helper.py check_build libsigscan
